# Copied into the container as /etc/cli-openai-proxy/gateway.env on boot.
# Map each API key to a stable tenant/user identity.
USER_API_KEYS='[{"key":"replace-with-a-long-random-key","tenantId":"default","userId":"user-a"}]'
