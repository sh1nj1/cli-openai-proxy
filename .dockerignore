# Root build context (used for both the production image and the per-user
# Linux worker integration test, which builds this same image). Host
# artifacts must not leak in: node_modules holds host-platform natives and
# dist may be stale; both are rebuilt inside the image.
.git
node_modules
dist
coverage
tmp
*.log
deploy/docker/gateway.env
